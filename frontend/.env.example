# Revenue Rescue Engine — frontend environment template.
# Copy to frontend/.env (gitignored) and fill in real values.
# The Razorpay KEY_ID is public (it is embedded in the checkout script);
# the KEY_SECRET must NEVER appear here or anywhere in frontend code.
VITE_RAZORPAY_KEY_ID=rzp_test_your_key_id_here
